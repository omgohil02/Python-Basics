#variable in python 
#Variables act as named storage containers for data. Python is dynamically typed, meaning you do not need to explicitly declare a variable's type before using it.

x = 5                # Integer (whole number)
pi = 3.14            # Float (decimal number)
name = "om"          # String (Text Data)
is_active = True     # Boolean (True or False)
